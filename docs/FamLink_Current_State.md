# FamLink - Current State (shared, cross-tool)

**Canonical resume point for any agent (Claude Code, Codex, etc.).** Read this first. Process rules live in `docs/FamLink_Agent_Rules.md`. If this doc conflicts with the live code, trust the code and update this doc.

| Field | Value |
|---|---|
| Last updated | 2026-08-07 (**W1 PR-2 planning IN PROGRESS — not execution-ready.** Spec amended: §2 decisions 8 (consent gates a reachable autonomous party), 9 (`Person.createdByFamilyGroupId` provenance gate), 10 (seat billing decoupled from consent → reconciled from actual membership). PR-2 plan drafted (`docs/superpowers/plans/2026-08-07-w1-pr2-consent-flows.md`) + 2 council rounds (15→14 BLOCKERs, did NOT converge); blocked on a round-3 revision + a billing-reconciliation slice. Repo cleanup done (`.playwright-mcp` untracked+gitignored). See Work Completed 2026-08-07. Prior: W1 PR-1 MERGED+DEPLOYED 2026-07-22.) |
| Branch | `main`; feature branch `p3-04-w1-household-family-m2m-schema-core` pushed → **PR #12 OPEN, DO NOT MERGE YET** (see Checkpoint). Worktree preserved at `.claude/worktrees/p3-04-w1-pr1`. |
| Checkpoint | **W1 PR-1 (Household↔Family M2M schema core) BUILT — PR #12 open, NOT merged.** https://github.com/huskermac/FamLink/pull/12 — 14 commits (`1c30273`..`9e121ca`, base `ecb37f3`), phase **P3-04**. Replaces `Household.familyGroupId` with a `HouseholdFamily` M2M join (+ `HouseholdAuditEntry`); household authz → **admin of ANY linked family** with an audit entry in the same transaction as every mutation; new `GET /households/:id`, `POST /:id/unlink` (min-1 → `409 LAST_LINK` unless `destroy:true`, serialized with `SELECT..FOR UPDATE`), `GET /:id/audit`; household-invite event visibility tightened to the viewer's own active membership in the **event's** family. Expand→contract in one PR (2 migrations). **Behavior identical for today's single-linked households; no API creates a second link until PR 2.** **🚫 MERGE BLOCKED ON: W3b prod deploy (Open Blockers #6) must happen FIRST** — prod is already 1 migration behind, and `migrate deploy` would otherwise apply 3 migrations + 2 never-deployed features at once. **Next after merge: W1 PR 2 (consent flows / `LinkRequest`) — plan not yet written.** |
| Local verification | PASS (W1 PR-1, 2026-07-15, controller-verified on own runs not implementer claims): full API suite **524/524** (45 files), full web suite **169/169** (coverage **87.29%** lines, threshold 80; web untouched), repo-root `type-check` 6/6 clean, `lint` 0 errors (34 known api warnings), `git diff --check` clean. PR #12 CI: see PR. Prior W3b baseline: API 478/478. |

---

## Current Phase / Status

**Phase 3 - Family-Model Reframe (P3-02 / P3-03), IN PROGRESS.** Design council-converged (`docs/FamLink_Design_Family_Model_Reframe.md`). The reframe replaces the older P3 ideas (Image Pipeline, Group Chat, Layer-3 AI) which are re-sequenced behind it.

**Shipped and merged:**
- **W3b — passive SMS onboarding** (P3-03, PR #11, squash `95e876a`, 2026-07-14): `SmsConsent` model (unique `phoneNormalized`, additive migration `20260710183002_sms_consent`) + consent lib (`isPhoneSuppressed`/`recordSmsOptOut`/`recordSmsOptIn`); suppression gate at both Twilio send sites (live `NotificationService.sendSms` + dormant `InvitationService.sendEventInvitations`); compliance footer (verbatim spec §7) + 320-char budget via shared GSM-7-safe `buildBudgetedSmsBody` (ASCII `...` marker — Steve-approved amendment; U+2026 would force UCS-2 5-segment billing); inbound handler `smsInbound.ts` (STOP/START/HELP/Y/N/UNKNOWN, idempotent most-recent-invitation resolution, phone verification w/ different-person conflict guard, HELP always replies per CTIA) — **all invitation queries scoped `guestPhone IS NOT NULL`** so SMS replies can never touch W3a cross-family participation invites (final-review Important, fixed + regression-tested); shared `rsvpWindow.rsvpClosed` used by web-link RSVP (`guest.ts`) and SMS; signature-verified webhook route `twilioSms.ts` (validation before any DB work, per-request URL from new `API_PUBLIC_URL` env incl. `turbo.json` `globalPassThroughEnv`, TwiML SDK-escaped + test-locked) mounted before `express.json`. Spec: `docs/superpowers/specs/2026-07-10-w3b-passive-sms-onboarding-design.md`; plan: `docs/superpowers/plans/2026-07-10-w3b-passive-sms-onboarding.md` (both amended in-branch).
- **W2** - per-person AI entitlement (P3-02, PR #1).
- **W2b** - entitlement surfacing + foreign-context AI throttle + mobile upsell (P3-02, PR #2).
- **W3a-API** - cross-family event participation backend (P3-03, PR #3): `EventParticipant` grant + roles, `resolveEventAccess`, cross-family invite/accept/decline/revoke/role, participant RSVP + per-item task contributions, `ForeignInvitedEventDTO`, participant-scoped notifications.
- **Contact Identity Foundation - Plan A (backbone)** (P3-03, PR #4, merge `f45f11c`; prod completed 2026-06-29): contact normalization helpers, additive `Person` normalized/verified contact columns, verified-only partial unique indexes, canonical `findOrCreatePersonByContact`, Clerk webhook normalized+verified email writes, guest invite resolver routing through canonical contact identity, and one-time Clerk verified-contact backfill. Spec: `docs/superpowers/specs/2026-06-25-contact-identity-foundation-design.md`; plan: `docs/superpowers/plans/2026-06-25-contact-identity-foundation-a-backbone.md`.
- **Contact Identity Foundation - Plan B (merge engine)** (P3-03, PR #5, merge `a034137`): transactional `mergePersons` (re-points every Person-referencing column incl. logical no-FK columns, compound-unique dedupe, delete; refuses to delete an account; idempotent), dependent-safety gate + full-name corroboration (`selectMergeableContactPerson`/`nameCorroborates`), and a retry-safe Clerk `user.created` post-upsert consolidation that merges a safe contact-only guest into the new account (guest invitation/RSVP history reconciles via `linkedPersonId`). Logs-only observability, no new table. Council-validated (Codex, 2 rounds) + final opus review READY TO MERGE. Spec: `docs/superpowers/specs/2026-06-26-cif-plan-b-merge-engine-design.md`; plan: `docs/superpowers/plans/2026-06-26-cif-plan-b-merge-engine.md`.

- **W3a-UI-mobile - PR 2 (organizer slice)** (P3-03, PR #10, squash `ac2f155`): mobile-only (organizer API already shipped in W3a-API). Client hooks (`useInviteeSuggestions`/`useSendInvitations`/`useParticipants`/`useRevokeParticipant`/`useSetParticipantRole`/`useIsFamilyAdmin`); **invite screen** at sibling route `apps/mobile/app/(tabs)/events/invite/[eventId].tsx` (members + cross-family suggestions w/ admin-gated "Make event admin" toggle + external guest, neutral "Invitations created" copy, foreign-event guard that also gates suggestions `enabled:false`); **Invite button** on `OwnEventDetail`; **participant management** section on `OwnEventDetail` (list + role badge, REVOKED muted, admin-gated revoke-behind-confirm + role toggle). Client admin-gating = family roles only (`useIsFamilyAdmin`), server authoritative. Built via council-reviewed plan (2 rounds) + subagent-driven TDD (4 tasks) + opus whole-branch review. **Deferred (backlog, non-blocking):** spec §5.1 per-row "already invited" summary line (flat success shown instead); phone-only guest test. Spec §5: `docs/superpowers/specs/2026-07-06-w3a-ui-mobile-design.md`; plan: `docs/superpowers/plans/2026-07-06-w3a-ui-mobile-pr2-organizer-slice.md`.
- **W3a-UI-mobile - PR 1 (participant slice)** (P3-03, PR #9, squash `d1abca7`): 3 additive API changes — atomic `POST /events/:eventId/items/:itemId/claim` (status-guard `updateMany`; fixes claiming, previously impossible via PATCH/the broken potluck PUT), `myRsvp` on `ForeignInvitedEventDTO` (requester-scoped; also closes W3a-UI-web deferred item #2), `GET /events/participating` (allowlist-only discovery, multi-family dedupe, registered before `/:eventId`). Mobile: foreign types + `isForeignEvent` discriminator + `useParticipatingEvents`; items hooks on the `/items` model; events list merge with "Guest" badge + partial-failure isolation; event-detail split into thin route + `OwnEventDetail` + `ForeignEventDetail` (foreign viewer renders DTO-only, no owner surfaces); isError-first stale-cache guard (`retry:false`, no removeQueries loop); **first mobile CI job** (lint+type-check+jest, wired into `build` needs). No migration. Spec: `docs/superpowers/specs/2026-07-06-w3a-ui-mobile-design.md`; plan: `docs/superpowers/plans/2026-07-06-w3a-ui-mobile-pr1-participant-slice.md`. **PR 2 (organizer slice) deferred as the committed follow-on.**
- **W3a-UI-web** - cross-family event participation web UI (P3-03, PR #6, merge `e7952e3`): **day-1 guest invitation delivery** (direct email/SMS of the `/rsvp/{token}` link, isolation-safe copy), **participant revival** (accept widened to {PENDING,DECLINED}, never ACCEPTED — admin revoke stays authoritative), identity-bound `GET /events/participation/preview`, owning-member-only `GET /:eventId/participants`, `isOwn` flag on foreign items, and web surfaces: invite-page cross-family roles (admin-gated), `/events/accept` page (state matrix), foreign-DTO participant viewer (add + delete-own tasks), and owning-member participant management. Spec: `docs/superpowers/specs/2026-06-25-w3a-ui-web-cross-family-participation-design.md`; plan: `docs/superpowers/plans/2026-06-30-w3a-ui-web-cross-family-participation.md`.

## Reordered Roadmap (Canonical)

1. ~~Merge or PR **Contact Identity Foundation Plan A**~~ - **DONE** (PR #4 merged `f45f11c`).
2. ~~Write and execute **Contact Identity Foundation Plan B (merge engine)**~~ - **DONE** (PR #5 merged `a034137`).
3. ~~**W3a-UI-web**~~ - **DONE, MERGED** (PR #6, merge `e7952e3`; branch deleted). Stale `apps/web/src/` tree removed via PR #7 (merge `35be8f7`).
4. ~~**W3a-UI-mobile**~~ — **DONE, both PRs MERGED** (PR #9 participant slice `d1abca7`; PR #10 organizer slice `ac2f155`; both branches deleted).
5. ~~**W3b** - passive SMS onboarding~~ — **DONE, MERGED** (PR #11, squash `95e876a`; branch + worktree deleted). Prod deploy steps pending Steve (Open Blockers #6).
6. **W1** — Household↔Family M2M reframe (migration-heavy). **IN PROGRESS:** PR 1 (schema core) **MERGED + DEPLOYED to prod** (PR #12, squash `e36e2e7`; both migrations applied, backfill verified 5/5/5). Remaining slices (spec §10): **PR 2 consent flows (`LinkRequest`) — plan not yet written, next to write/execute**; PR 3 web UI; PR 4 mobile UI.
7. **W4** - Pro Organizer beta (non-family event admin + B2B billing).

## Work Completed Since Last Shared-State Update

2026-08-07 **W1 PR-2 (consent flows / `LinkRequest`) planning — spec amended + plan drafted, NOT execution-ready. No app code changed.**
- **Repo cleanup** (`5e21a52`, pushed): `.playwright-mcp/` (76 committed transient browser-MCP artifacts) untracked + gitignored; AGENTS.md/CLAUDE.md GitNexus symbol-count bumps folded in.
- **Spec amended** (`a00595e`, `8b42bd2` — both **unpushed**): three new §2 locked decisions. **8** — consent gates a *reachable autonomous party*: active account → in-app consent; passive with a contact detail → token consent; passive with no contact → data entry, no gate (narrows §3.2/§6.2/§6.3). **9** — `Person` gains `createdByFamilyGroupId`; data-entry direct-add allowed only for records the requester's family authored (closes a foreign-passive-attach hole the council found). **10** — seat billing **decoupled from consent**: reconciled from the family's true active-member count via an idempotent reconciliation on membership change + Stripe webhook; the consent/accept flow never calls Stripe inline and never blocks acceptance on billing (supersedes the per-mutation `checkSeatExpansion` 402-confirm).
- **PR-2 plan drafted** (`286f9fd`, **unpushed**): `docs/superpowers/plans/2026-08-07-w1-pr2-consent-flows.md`, 10–11 TDD tasks. **Two Codex council rounds: 15 then 14 BLOCKERs — did NOT converge** (capped at 2 rounds per the skill). Round-1 structural fixes landed + confirmed (conditional-claim grant, lock-revalidated household authority, correct audit attribution, no pre-accept `EventParticipant`). Non-convergence traced to **two design gaps**, now **resolved by Steve** (decisions 9 + 10 above). Plan carries a **⚠️ NOT EXECUTION-READY banner**; ~12 mechanical round-2 BLOCKERs remain (delivery-wiring regression, atomic token+verify, snapshot delivered contact, minor-branch ordering, attestation-before-minor, channel fallback, inbox target-name, owner-id echo, carry-in/household create-time authz, expiry-sweep-on-create, token-replay idempotency, no-op-grant guard, per-task `detect_changes`).
- Blast-radius (planning rule): `activeFamilyMembership` MEDIUM (20 upstream), `householdIdsForPerson` HIGH — neither modified by PR 2 (PR-1 already tightened them); PR 2's risk is *who can create* the rows they trust. GitNexus index code-fresh (only docs/config changed since the 2026-07-24 analyze).

2026-07-22 **W1 PR-1 MERGED + DEPLOYED to prod; W3b migration confirmed; full deploy chain closed (ops only, no code).** Continuation of the 07-21 park.
- **W3b migration CONFIRMED on prod.** The Railway Deploy log (not Build log) for the `API_PUBLIC_URL`/`TWILIO_PHONE_NUMBER` redeploy showed datasource `database "railway"` at `gondola.proxy.rlwy.net:15778`, `21 migrations found`, **`No pending migrations to apply`** (SmsConsent already present), API listening, `/health` 200. So prod was caught up → migration-hygiene block on PR #12 cleared.
- **PR #12 MERGED** via squash → `e36e2e7` on `main` (`gh pr merge 12 --squash --delete-branch`). CI on the merge commit green (lint-and-typecheck, test, mobile, build all ✅, run `29926162075`). Worktree `.claude/worktrees/p3-04-w1-pr1` removed + branch `p3-04-w1-household-family-m2m-schema-core` deleted (worktree `remove` left a non-empty dir on Windows → manual `rm -rf` + `git worktree prune`). Local `main` rebased so the two 07-21 docs checkpoints sit on top of the merge (local is **ahead of origin by the docs commits — still unpushed**).
- **W1 prod deploy SUCCEEDED.** Merge → CI green → Railway deploy. Deploy log: `23 migrations found`, applied **both** `20260715132007_household_family_m2m_expand` and `20260715150000_household_family_m2m_contract`, "All migrations have been successfully applied", API up, `/health` 200. The expand→contract-in-one-release went through with no error window observed (3 families / 12 persons scale).
- **Backfill VERIFIED on prod.** `verifyHouseholdBackfill.ts` (run from `apps/api` against prod `DATABASE_URL`) → `{"households":5,"householdsWithAtLeastOneLink":5,"totalLinks":5}` — exactly one link per household, no orphans. Invariant holds (valid until PR 2 introduces multi-links).
- **Gotchas hit running the verify script against prod (all resolved):** (1) bare `npx tsx …` hits **local** `famlink_dev`, not prod — must inject prod `DATABASE_URL`; the script prints only counts so it does NOT reveal which DB it hit — targeting must be deliberate. (2) The local generated Prisma client was **stale** (knew `familyGroupId`/`familyGroup`, not the new `families` M2M relation) → `Unknown argument 'families'`; fixed by `npm run build` in `packages/db` (regenerate + copy to `dist`). Prod is unaffected (Railway regenerates on build). (3) `DATABASE_URL` must be set in the **same** shell session as the run or you get `SASL: client password must be a string`. Prod Postgres public proxy = `gondola.proxy.rlwy.net:15778` (the `*.railway.internal` host is unreachable off-Railway).
- **Prod smoke-checked post-deploy (2026-07-22):** `GET /health` → 200 `{"status":"ok","db":"ok"}`; `GET /` → 200; `POST /api/v1/webhooks/twilio/sms` with no signature → **400 `{"error":"Invalid signature"}`** (route mounted + signature-validating, i.e. `API_PUBLIC_URL` wired). Railway/API side of W3b is correctly configured; the exact `API_PUBLIC_URL` match is only fully proven by a real signed Twilio request, and the Twilio-console webhook attach + toll-free verification (#7) remain the only SMS gaps (Twilio-side, not Railway).
- **NEW EXPOSURE (Blocker #8):** the full prod Postgres connection string **including password** was pasted into the chat transcript during this run → **rotate the Railway Postgres password** (same procedure as 2026-06-18).
- **GitNexus is now stale** — `main` advanced past `e85e9b4` to `e36e2e7` (+ docs). **Re-analyze on the new `main`** (per CLAUDE.md session-end rule) so the index knows `HouseholdFamily`/`householdAccess.ts`/etc.

2026-07-21 **W3b prod deploy STARTED, then PARKED at the Twilio step (Steve's request). No code changes.** Progress on Open Blockers #6:
- **(a) Migration — UNCONFIRMED on prod (top resume item).** A first `npx prisma migrate deploy` was run from Steve's local shell; it hit **local** `famlink_dev` at `localhost:5432` (root `.env` `DATABASE_URL` points local since the 06-18 split) and reported "No pending migrations" — a **no-op for prod**. Then `API_PUBLIC_URL` was set in Railway → API service → Variables and a redeploy was triggered ("deployment in progress"). **The prod migration application was never verified** — the Railway deploy log was not checked to confirm `20260710183002_sms_consent` applied + `/health` green. Prod auto-runs the migration via the API `start` command (`prisma migrate deploy --config=packages/db/prisma.config.ts && node …`), so the redeploy *should* have applied it, but **confirm the deploy log first thing on resume.** The migration is a pure additive `CREATE TABLE "SmsConsent"` + unique index (verified) — zero data risk.
- **(b) `API_PUBLIC_URL` set** in Railway prod to `https://famlink-api-production.up.railway.app` (env-var name + webhook path verified against `env.ts`/`twilioSms.ts`/`server.ts`). **TODO: mirror it into Infisical `prod`** (unconfirmed whether done).
- **(c)–(e) Twilio BLOCKED — the prod number is GONE.** Prod's configured `TWILIO_PHONE_NUMBER=+18556480251` (an 855 toll-free) **no longer exists in the Twilio account** — not found in an Active Numbers search; cause unknown (released?). This means **outbound** prod SMS would currently fail too, not just inbound. **RESOLVED (number side):** Steve **purchased a replacement toll-free `+18444235115` ((844) 423-5115)** and set `TWILIO_PHONE_NUMBER` to it in **both Railway and Infisical**. (Setting the Railway var triggers a fresh redeploy — so the *latest* Railway deploy is now the one to check for the migration in (a).) Still a toll-free number → **toll-free verification still required** (see #7); parked at the verification form, which asks for a **legal entity name** (Steve has none).
- **IMPORTANT reframe (don't lose this):** PR #12's merge block is about **migration hygiene** — getting prod onto `SmsConsent` so PR #12's later deploy is a clean **2-migration** change (W1 expand+contract) instead of **3**. That is satisfied by the redeploy applying the migration — **independent of Twilio.** So the moment the deploy log confirms `SmsConsent` applied, **PR #12 is free to merge.** The Twilio number + toll-free verification is a **separate feature-enablement track that does NOT gate the merge.**
- Toll-free verification surfaced the broader launch-readiness cluster (no legal entity / contested name / no real domain / sandbox email / Clerk test instance) — captured as new Blocker #7. Claude offered to draft the verification submission (use-case + sample messages leaning on W3b's built-in compliance); not yet done.

2026-07-15 **W1 PR-1 (Household↔Family M2M schema core) BUILT → PR #12 OPEN (not merged).** Phase tag advanced to **P3-04**. Executed the council-converged plan via subagent-driven development: 5 TDD tasks (fresh implementer + spec/quality review gate each) → whole-branch opus review → 2 fix waves. 14 commits `1c30273`..`9e121ca` on `p3-04-w1-household-family-m2m-schema-core`, base `ecb37f3`.

- **Tasks:** (1) `1c30273` `HouseholdFamily` + `HouseholdAuditEntry` models, expand migration w/ hand-added backfill INSERT, creation dual-write, `verifyHouseholdBackfill.ts`, merge-map entries; (2) `0a2531e` `lib/householdAccess.ts` (`anyLinkedMembership`/`householdViewer`/`householdAdmin`/`linkedFamilies`/`writeHouseholdAudit`); (3) `c60bc78`+`f16592f`+`d6b8c5d` household routes on any-linked-family authz + audit + GET + unlink/destroy; (4) `c91075f` family-side reads via join + eventVisibility invariant-3 tightening + aiTools; (5) `bc0ba04` contract (drop the FK). Fix waves: `c8b2dc6`,`f7c5fdc`,`3045532`,`beebf66` then `3ca8f0c`,`2a9e6de`,`9e121ca`.
- **THE INVARIANT-3 LEAK WAS REAL.** RED was a genuine assertion failure on a real DB — pre-fix `canViewEvent` returned VISIBLE for a resident of a shared household who was NOT a member of the event's family (family A's PRIVATE event visible to a family-B-only resident). The council's round-1 BLOCKER caught a live cross-family leak. Fix is membership-based against the **event's** family (a link-based filter would pass in exactly the leak scenario). Verified the tightening drops ONLY the household-invite branch — creator/RSVP/ACTIVE-participant/individual-invitation grants untouched.
- **The whole-branch review earned its keep again** (as on W3b): it found **2 isolation leaks living in the SEAMS between tasks** — each per-task reviewer was correct in isolation, and both leaks are **structurally invisible in PR-2's diff**, which is what makes them reachable while touching none of these files. (1) `GET /families/:familyId` returned residents via `serializePersonBrief` (Clerk `userId` + DOB + photo) — Task 3 had widened the resident gate to any-linked-family. (2) The audit endpoint returned raw foreign actor ids; a follow-up re-review then caught that suppressing the actor ids still left a foreign person id flowing through the `changes` field (the unlink cascade recorded `personId`) — closed by recording the display name at the write site instead.
- **Steve decisions this session (4):** (a) **cascade-remove residents stranded by an unlink** — spec gap, fixed in-branch, narrow criterion, mutation-verified; (b) **consolidate `anyLinkedMembership`** rather than keep the plan-mandated duplicate; (c) **Seam 1 fix = filter residents to the requesting family** (keeps `PersonBrief` shape; reshaping would break web's `MemberCard`); (d) **Seam 2 = resolve audit actors to display names + own-family-only ids** → **spec §3.3 amended** (see below).
- **SPEC AMENDED (`beebf66`, `9e121ca`):** §3.3/§7.1 genuinely conflicted — §3.3 said the audit log is readable by all linked admins and called `actorFamilyGroupId` a snapshot; §7.1 said never another family's ids. The council-converged plan mandated the leaky shape verbatim without noting the conflict. **Steve resolved restrictively:** access unchanged, but ids disclosed only when the viewer is an active member of that entry's family; otherwise display name + family name with the id keys **absent**. Also noted: `actorFamilyName` can name a no-longer-linked family (historical snapshot; accepted — names are permitted by invariant 1).
- **One accepted LIVE behavior change:** `GET /families/:familyId` now scopes household residents to the requesting family's members. This is **NOT a no-op** (an earlier claim to the contrary was wrong and is corrected): `DELETE /families/:familyId/members/:personId` hard-deletes the `FamilyMember` row but leaves `HouseholdMember`, so ex-members currently appear in the family view with DOB + Clerk id and now drop out. Steve accepted; pinned with a regression test. They remain visible/removable via `GET /households/:id`.
- **Premise correction worth keeping:** the assumption that rows written in one Prisma transaction share PostgreSQL's `CURRENT_TIMESTAMP` is **FALSE for Prisma 7.7** — it generates `now()` **client-side**. Proven with raw SQL when a test that depended on the tie wouldn't bite. Relevant to any future ordering/tie reasoning.
- Verified (controller's own runs, not implementer claims): API **524/524**, web **169/169 @ 87.29%**, type-check 6/6, lint 0 errors, `git diff --check` clean.

2026-07-14 (later same day) **W1 designed + PR 1 planned (docs only, no code).** Phase gate run (all open items explicitly deferred by Steve; W3b deploy flagged as parallel to-do). Brainstorm → Steve locked 6 product decisions (spec §2: A+B+C scope; any-admin+audit; W3a-participation escalation for household invites; guardian=adult-admin-of-minor's-family; teens=minors; request→grant architecture) → spec `3f15f2e` → PR-1 plan `52e7096` → council round 1 (`1dd26fd`, 6 BLOCKERs fixed: eventVisibility tightening was wrong — viewer-membership in event's family is the rule, not household-linked-to-family; unlink cross-tenant destroy hole; min-1 race → FOR UPDATE; actorAdminFamily wrong-membership; unaudited creation; linkedFamilies id leak → viewer-scoped ids + spec §6.1 amended) → round 2 (`ca37977`, 3 mechanical BLOCKERs fixed, converged). **Phase tag advances to P3-04 with the W1 PR-1 execution session.**

2026-07-14 **W3b built + merged** (PR #11, squash `95e876a`; branch `p3-03-w3b-passive-sms-onboarding` + worktree deleted; local naming-shortlist doc commit rebased on top → main HEAD `de8ed98`). Full workflow: spec (council 2 rounds, `21ea09c` 2026-07-10) → plan (council 2 rounds, `0bd4a98`) → subagent-driven TDD, 6 tasks. **Sessions 2026-07-10 froze twice mid-Task-3** (once mid-implementation, once at review dispatch); resumed 2026-07-14 from the SDD ledger + git with zero work lost. Mid-branch Steve-approved amendments: (1) valid-NANP test literals (555 area code rejected by libphonenumber, found Task 1, `9db5692`); (2) ASCII `...` truncation marker + shared `buildBudgetedSmsBody` (Task 3 review found the plan's U+2026 marker forces UCS-2 → 5 billable segments, and the plan-mandated duplicated budget algorithm — Steve approved both fixes, `bdcbde4`+`f0bdf8f`). **Final whole-branch opus review found 1 Important the per-task gates couldn't see:** inbound queries (`findLatestInvitation`, STOP `updateMany`, `hasPendingInvitation`) filtered on `linkedPersonId` alone and also matched W3a cross-family participation invites (species `famlinkUser` never sets `guestPhone`) — a texted "Y" could ACCEPT a participation invite without its `EventParticipant` grant (soft-lock) and STOP could decline pending in-app invites. Fixed `46a91a7` (`guestPhone: {not: null}` at all 3 sites + 2 RED-verified regression tests); re-review **READY TO MERGE**. Spec §5 amended accordingly (`c50f0d0`). Verified: API 478/478, web 169/169 @ 87.29% coverage, type-check 6/6, lint 0 errors; PR CI green (incl. mobile). Also this stream (pre-committed docs): competitive naming shortlist `4642ef0` (2026-07-09, decision pending, no rename authorized).

2026-07-06 **W3a-UI-mobile PR 2 (organizer slice) built + merged** (PR #10, squash `ac2f155`; branch `p3-03-w3a-ui-mobile-organizer` deleted). Mobile-only. Same workflow: spec §5 (already council-approved) → plan (2 Codex council rounds, converged) → subagent-driven TDD (4 tasks: hooks → invite screen+button → participant mgmt → verify) → whole-branch opus review (READY TO MERGE, no Critical/Important). Branch commits `834670c`..`1fd6259`. One Important task-review finding (invite foreign-event guard had no test) fixed mid-flight (`4bbf4cf`, invite suite 5/5) before proceeding. Council caught real issues both plan rounds (Alert-backed revoke test couldn't pass as written; `useInviteeSuggestions` needed an `enabled` gate; existing `OwnEventDetail.test.tsx` needed all 4 new hook mocks or `jest.requireActual` runs real mutation hooks with no provider). Deferred to backlog: spec §5.1 per-row invite summary line, phone-only guest test. Verified: mobile 53/53, API 423/423, web 169/169 @ 87.29%, type-check 6/6 clean, lint 0 errors. **W3a-UI-mobile now COMPLETE (both PRs).**

2026-07-06 **W3a-UI-mobile PR 1 (participant slice) built + merged** (PR #9, squash `d1abca7`; branch deleted). Full workflow: brainstorm → spec (2 Codex council rounds) → plan (2 Codex council rounds) → subagent-driven TDD across 11 tasks (0–10; fresh implementer + spec/quality review gate each) → whole-branch opus review (READY TO MERGE, no Critical/Important). Branch commits `344731a`..`459d3ab`. Two spec deviations discovered in planning/review and folded in: (1) claim needed its own additive endpoint (`§3.3`) — PATCH-assign was impossible (`PatchItemSchema` has no assignee; `authorizeItemMutation` is admin-or-creator); (2) stale-cache suppression became an isError-first render guard, not cache eviction (removeQueries refetch-loops on a persistent 403). Task 0 also fixed a latent gap: mobile `tsconfig`/lint only covered `app/**`, so `hooks/`/`lib/`/`providers/`/`components/` were never type-checked or linted. Verified: API 423/423, web pass @ 87.29%, mobile 37/37, repo type-check 6/6 clean, lint 0 errors.

2026-07-06 **Railway Agent chat exposure — all six secrets rotated (ops, no app code changes).** Closed the 2026-07-02 exposure (full April `.env` paste in Railway's AI-agent chat). Rotated + verified, old values confirmed dead: `RESEND_API_KEY` (two new keys prod/dev, old deleted), `TWILIO_AUTH_TOKEN` (secondary-token promotion, old primary deleted), `CLERK_SECRET_KEY` (Clerk "+ Add new key" on the dev instance; updated Infisical/Railway/**Vercel**/local; old deleted — zero-downtime), `CLERK_WEBHOOK_SECRET` (new webhook endpoint for a fresh `whsec_`, old endpoint deleted), `GUEST_TOKEN_SECRET` (app-generated 64-char, invalidated 1 outstanding test RSVP link only), `FIREBASE_PRIVATE_KEY` (new service-account key dated today, old April key deleted). All validated status-only (no secret values in transcript): Resend/Twilio/Clerk-secret → 200, Firebase → real Google OAuth token issued, Clerk webhook route → 400 on bad sig, prod health + Vercel web → 200. Audit log: `docs/FamLink_Secrets_Runbook.md`. **Residual pending Steve:** delete the Railway agent conversation; optional Clerk end-to-end confirms (web login + webhook test event). Also this session: committed the untracked Stripe skills + `skills-lock.json` and gitignored `.hermes/` (commit `c9a0a56`).

2026-07-02 **Key-hygiene + Stripe live cutover session (ops, no code changes):**
- **Cloudflare R2 token rotated to bucket-scoped** (`famlink-photos-scoped-2026-07`, Object R/W on `famlink-photos` only). Set in Infisical `dev`+`prod` via clipboard flow (values never in transcript), Railway prod updated, verified with a real S3 put/get/list/delete round-trip, old April token revoked. Local `.env` still holds the dead old values — clean up eventually (only `npm run dev:infisical` matters).
- **`NODE_ENV=production` in Railway prod** (was `development` since launch → errorHandler leaked error internals to API clients).
- **Stripe live cutover COMPLETE:** account activated (Steve); live products FamLink Family $4.99/mo (`price_1TooXuEpMILkAfP5GtcOZVAf`), Active Seat $1.99/mo (`price_1TooYaEpMILkAfP5CVevcjVN`), Unlimited $14.99/mo (`price_1TooZVEpMILkAfP5skzRY3Bj`); restricted live key `famlink-api-prod-live` (Customers/Checkout Sessions/Subscriptions/Invoices/Customer Portal Write + Products Read — note the API needs Invoices/Portal/Prices, which the old test-key scope list missed); live webhook `famlink-api-production` (`we_1ToogsEpMILkAfP5MkA5NnyZ`, API version `2026-05-27.dahlia` matching the SDK pin, 5 events); prod `PricingTier` updated to live **monthly** prices (fixing a latent yearly-base/monthly-seat interval mix that would have broken seat-overflow checkout); stale test-mode `stripeCustomerId` cleared. Customer portal already matched ops-doc config. **Smoke test passed with a real checkout:** webhook 2/2 delivered (0 failed), `FamilySubscription` → BASE/TRIALING (14-day trial, first charge ~Jul 16), live customer+subscription attached. The web app nav has no billing link — smoke test used `/billing/plans` directly (small UI gap, noted).
- **Vercel plugin confirmed disabled** (`settings.json` `vercel-plugin@vercel-vercel-plugin: false`); stale TODO memory removed.
- **New security findings** (see Open Blockers): Railway Agent chat contains an April 2026 full `.env` paste (6 still-live secrets exposed — rotation decision pending); prod auth runs on a Clerk TEST instance; `RESEND_FROM_DOMAIN=resend.dev` (sandbox) blocks guest email to real guests pending a real domain.

2026-07-01 **Secrets management hardening — LIVE** (PR #8 merged `3638758`, plus 4 follow-up fix commits `53de0ba`..`17d6b54` on `main` from real rollout). `docs/FamLink_Secrets_Runbook.md` (full secret inventory + standard rotation procedure + audit log), `scripts/secrets/import-to-infisical.sh`, `npm run dev:infisical`, `.env.example` header notes, governance rule in `FamLink_Agent_Rules.md`. Steve ran the actual rollout end-to-end; it surfaced and fixed 4 real bugs: (1) `.sh` files silently routed through PowerShell's file association instead of running as bash — needed explicit `& "C:\Program Files\Git\bin\bash.exe"` invocation; (2) `--path="/"` in `infisical secrets set` got MSYS-rewritten into the Git install path on Git Bash for Windows, breaking every call with a 400 — fixed by dropping `--path` entirely (commit `b9c21d4`); (3) Infisical hard-rejects empty-valued secrets, which previously aborted the whole import script mid-run under `set -e` — fixed to skip-and-warn instead (commit `ac8dde0`); (4) `turbo.json`'s pre-existing `globalPassThroughEnv` (just `DATABASE_URL`/`TEST_DATABASE_URL`) silently switches Turborepo into Strict Environment Variable Mode, stripping every other Infisical-injected secret before it reached `@famlink/api` — invisible for 33/35 secrets that also existed in local `.env` as a fallback, only surfaced once `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` (Infisical-only, no local fallback) hit Zod "Required" — fixed by listing every `env.ts` variable in `globalPassThroughEnv` (commit `17d6b54`). **Verified end-to-end:** `npm run dev:infisical` boots the API cleanly, `GET /health` → 200. Spec: `docs/superpowers/specs/2026-07-01-secrets-management-design.md`; plan: `docs/superpowers/plans/2026-07-01-secrets-management-hardening.md`.

**Still open (Tier-1 hardening, deferred):** Cloudflare R2 access token is still broad-account-scope — needs a bucket-scoped (`famlink-photos`-only) replacement token, same treatment as the Stripe restricted key.

**New finding, needs Steve's own follow-up (not done today, deliberately not rushed):** prod's `STRIPE_SECRET_KEY` is `rk_test_...` — **production billing is running in Stripe test mode, not live.** Confirmed real (not an artifact of today's work) — Steve confirmed this should actually be live. Follow-up: create an `rk_live_...` restricted key (same permission scope: Subscriptions/Customers/Checkout Sessions/Webhooks) + a live-mode webhook endpoint's `STRIPE_WEBHOOK_SECRET`, then rotate both into Railway prod when ready to accept real payments.

2026-07-01 **W3a-UI-web merged to `main`** via PR #6 (merge `e7952e3`); feature branch `p3-03-w3a-ui-web` deleted (local + remote). Follow-on **PR #7** (merge `35be8f7`, commit `573f0a2`) relocated the web tests out of the stale `apps/web/src/` duplicate tree and deleted that tree (repo-hygiene deferred item — done); its worktree/branch `claude/objective-galileo-2d532a` cleaned up. Repo now clean: only `main`, no lingering worktrees, remote pruned. **Pending Steve:** set `RESEND_*`/`TWILIO_*`/`WEB_APP_URL` in prod so guest invitation email/SMS actually send (no migration needed — W3a-UI-web was additive).

2026-06-30 **W3a-UI-web built** on branch `p3-03-w3a-ui-web` (11 commits `db0f2c7`..`07510ae`), READY TO MERGE. Flow: brainstorm → spec refresh + Codex council (no BLOCKERs) → plan + 2 Codex council rounds (round 2 clean) → subagent-driven execution (10 TDD tasks, fresh implementer + spec/quality reviewer each) → whole-branch opus review (READY TO MERGE, no Critical/Important). Commits: `db0f2c7` guest delivery, `aefc391` accept-revival, `78f1669` preview endpoint, `2c08405` participants list (owning-member-only), `f7c71af` foreign-item `isOwn`, `4aba71d` web client, `1b23ae2` invite-page roles, `d60ef99` accept page, `765e2bd` accept-deps comment fix, `38b49b3` foreign viewer, `07510ae` participant mgmt. A controller re-boundary moved the `InviteeEntry`/`getEventDetails` breaking type changes into their consumer tasks (7/9) to keep the workspace type-checking at every step. Pending Steve: prod has no new migration (additive only — `isOwn` is computed, no schema change); ensure `RESEND_*`/`TWILIO_*`/`WEB_APP_URL` env vars are set in prod for guest delivery to actually send.

2026-06-26 CIF Plan B (merge engine) **merged to `main`** via PR #5 (merge `a034137`); feature branch `p3-03-cif-plan-b-merge-engine` deleted (local + remote). Built via brainstorm -> spec -> plan -> council (Codex, 2 rounds; caught `EventPhoto.uploadedById` not `personId` + missed `AssistantMessage.personId`) -> subagent-driven execution (3 tasks) -> final opus whole-branch review (READY TO MERGE). Plan B commits `366ac4a` (mergePersons), `4dc1c98` (selection + corroboration), `5e25395` (guarded-adult test), `6e3b8b0` (webhook consolidation), `ac20560` (review follow-ups). Verified: branch API suite 403/403 (39 files), main baseline 381/381, tsc/lint clean.

2026-06-29 CIF Plan A production item **completed** from `main` HEAD `e41a6cc`. Pushed adapter metadata cleanup (`e41a6cc`), CI run 28372051672 passed (lint/typecheck, test, build), production Prisma deploy via Railway reported `No pending migrations to apply` against Railway PostgreSQL, and `backfillClerkContacts.ts --apply` updated 1 Person with 0 errors. Post-apply dry run reported `alreadyCurrent: 1`, `wouldUpdate: 0`, `errors: 0`.

2026-06-26 CIF Plan A **merged to `main`** via PR #4 (merge `f45f11c`); feature branch `codex/p3-03-cif-plan-a` deleted (local + remote). Plan A commits:
- `599fd6a` - contact normalization helper (`normalizeEmail`, `normalizePhone`) with tests.
- `1858792` - additive `Person` normalized/verified contact fields plus migration with verified-only partial unique indexes.
- `13f3feb` - canonical `findOrCreatePersonByContact` resolver with verified-match preference.
- `d8075ed` - Clerk webhook writes `emailNormalized` and `emailVerifiedAt`.
- `43160a0` - guest event invites route through canonical resolver.
- `a6ed399` - one-time Clerk verified-contact backfill script (`apps/api/src/scripts/backfillClerkContacts.ts`, dry-run by default; use `--apply` to write).

Earlier 2026-06-24/25 stream:
- W2b shipped -> **PR #2 merged**; W3a-API shipped -> **PR #3 merged** (final review fixed a roster-leak C1 + item-write leak I1, commit `372d98f`).
- Repo cleanup (`bb3bb7e`..`4c0819d`): deleted merged branches (local + remote); removed a stray committed worktree gitlink; gitignored `.claude/worktrees/`; untracked and gitignored `.claude/settings.local.json`.
- Wrote/committed: W3a-UI-web spec (`fad140f`), Contact Identity Foundation spec (`a716488`), CIF Plan A (`715b44c`), CLAUDE.md phase update (`33d950c`).
- CI fix (`9821542`): removed an unused `nonMember` var in `events.test.ts`; CI then green on `main`.
- Prod data check (Railway): 12 persons / 3 families / 0 contact collisions / 0 emails on `Person` (emails live in Clerk). The identity migration is additive, no wipe.

## Important Commits / PRs

- CIF Plan B commits (now on `main` via PR #5): `366ac4a`, `4dc1c98`, `5e25395`, `6e3b8b0`, `ac20560`; merge `a034137`.
- CIF Plan A commits (on `main` via PR #4): `599fd6a`, `1858792`, `13f3feb`, `d8075ed`, `43160a0`, `a6ed399`; merge `f45f11c`.
- PR #1 (W2), PR #2 (W2b), PR #3 (W3a-API), PR #4 (CIF Plan A), PR #5 (CIF Plan B) - all merged to `main`.
- Recent `main`: `a034137` (PR #5 merge), `81553d4` (Plan A checkpoint), `f45f11c` (PR #4 merge), `9821542` (CI fix).

## Verification Baseline

- **W3b (2026-07-14, merged `main`):** full API suite **478/478** (43 files; +49 tests over the 07-06 baseline incl. real-signature webhook e2e, real-DB handler tests, participation-invite regression pair, TwiML XML-escaping lock-in), web **169/169** @ **87.29%** lines (threshold 80, web untouched — note: `npm test --workspace=famlink-web` alone does NOT run the coverage gate; run `npx vitest run --coverage` in `apps/web`; also the workspace name is `famlink-web`, not `@famlink/web`), repo `type-check` 6/6 clean, `lint` 0 errors (34 known pre-existing warnings), `git diff --check` clean. PR #11 CI run 29364598163 green (lint-and-typecheck, test, mobile; build job conditionally skipped). GitNexus branch-compare scope was confined to `env.ts`/`invitationService`/`notificationService`/`server.ts` + new SMS modules.
- **W3a-UI-web (2026-06-30, branch `p3-03-w3a-ui-web`):** full API suite **415/415** (40 files), full web suite **169/169** (24→26 files; coverage **87.29%** lines vs 80 gate — initial build came in at 75.53% because per-task verification ran targeted `vitest run <file>` not the `--coverage` gate; fixed by commit `320b11d` adding real client/component tests, see `project_verification_coverage_gate` memory), repo-root `type-check` clean, repo-root `lint` 0 errors (34 pre-existing warnings), `git diff --check` clean. Additive only — no Prisma migration. Whole-branch opus review READY TO MERGE; all isolation invariants verified in code (foreign DTO carries no person ids; `/participants` owning-member-only; preview identity-bound + family-blind; guest copy structurally safe; revival never overrides revoke; `/events/accept` auth-gated).
- **CIF Plan B (2026-06-26, branch `p3-03-cif-plan-b-merge-engine`, now merged):** full API suite **403/403** (39 files); `main` pre-merge baseline 381/381; `tsc --noEmit` clean; `npm run lint` 0 errors (pre-existing warnings only). Additive — no schema migration. (Note: an implementer reported transient "33 pre-existing failures" that did NOT reproduce on a clean run — flake, not a regression.)
- **CI green at `main` HEAD `9821542`** - `lint-and-typecheck`, `test`, and `build` all passed in run 28183812961.
- CIF Plan A local verification on `codex/p3-03-cif-plan-a` (2026-06-26):
  - `npm.cmd test --workspace=@famlink/api` - PASS, 38 files / 381 tests.
  - `npm.cmd run type-check` - PASS across workspace.
  - `npm.cmd run lint` - PASS across workspace with existing warnings only.
  - `git diff --check` - PASS.
  - `mcp__gitnexus.detect_changes` vs `main` - medium branch diff risk; affected flows limited to new backfill-script chains.
- Earlier local baseline: API 368/368, web 112/112, mobile suite pass; `tsc --noEmit` clean across api/web/mobile.
- **Known non-blocking:** 34 eslint warnings in `apps/api` (mostly `@typescript-eslint/no-explicit-any` in test mocks plus stale `eslint-disable` directives); warnings do not fail CI.
- **Process note:** keep `npm run lint` in per-task/final verification loops.

## Next Recommended / Authorized Step

**W1 PR-2 plan is DRAFTED but NOT execution-ready (2026-08-07). The next step is a round-3 plan revision, gated by a billing-reconciliation slice.** In order:

1. **Billing-reconciliation slice (decision 10) — likely FIRST.** Build/verify the idempotent seat reconciliation (sets absolute Stripe seat quantity from the family's true active-member count; triggered on membership change + `customer.subscription.updated` webhook) and retire the per-mutation `checkSeatExpansion` 402-confirm. This is its own small plan; the consent PR depends on it (the consent flow must not call Stripe). Decide whether it precedes or accompanies PR 2.
2. **Round-3 revision of the W1 PR-2 plan** (`docs/superpowers/plans/2026-08-07-w1-pr2-consent-flows.md`): fold decisions 9 (Person `createdByFamilyGroupId` provenance + migration/backfill) and 10 (no inline Stripe) + clear the ~12 mechanical round-2 council BLOCKERs listed in the plan banner. Then remove the NOT-EXECUTION-READY banner. (Council capped at 2 rounds — a round-3 self-review against the banner list is sufficient; re-council optional.)
3. **Then execute** (subagent-driven TDD). **Carry the PR-2 constraints** (FOR UPDATE lock on household-link creation, re-validated under the lock; review brief must include `families.ts`/`aiTools.ts`/audit endpoint/`personIdentity.ts mergePersons`/`requestLogger.ts`).

**Older note (superseded by the above but kept for the PR-2 constraints):** the earlier "plan not yet written" is done. Original constraints still apply.
2. **Follow-ups not blocking PR 2 (Steve):** decide the Twilio toll-free verification path (Blocker #7 — "launch soon vs build/test?"); execute the local `.env` → Infisical consolidation (documented in the Secrets Runbook "Local environment architecture" section); replace the real (likely already-revoked) Firebase key in `apps/api/.env.test` with a placeholder (it's gitignored + never committed — low risk). *(Done: Postgres pw rotated #8; docs pushed; GitNexus re-analyzed; turbo.json passthrough completeness + apps/api/.env.example deprecated.)*

**W1 PR-1 deploy notes — HISTORICAL, deploy is DONE (kept for the rollback recipe if ever needed):**
- `prisma migrate deploy` applies both W1 migrations in order; the backfill runs while the FK column still exists, so migration-vs-migration ordering is safe **by construction**. That is NOT the risk.
- **The real risk is expand→contract shipping in ONE release.** Between `migrate deploy` finishing and the new container serving traffic, **old code runs against a schema with no `Household.familyGroupId`** — the old Prisma client selects that column on every household read, so every household read 500s for that window. At 3 families / 12 persons this is seconds of 5xx, but confirm Railway runs migrations as a **release command gated before cutover**.
- **Rollback is NOT a code-revert.** There is no down migration. Reverting the app without restoring the column leaves new code fine and old code broken. The data to reconstruct the column survives in `HouseholdFamily` — write that SQL *before* you need it.
- Safe failure direction: if migration 1 applies and migration 2 fails, `migrate deploy` aborts and old code keeps serving fine (column still there, still written).
- **After deploy:** run `npx tsx src/scripts/verifyHouseholdBackfill.ts` from `apps/api` against prod `DATABASE_URL` — exits non-zero and prints orphan household ids if any household lacks a link. Precondition: run before any second link exists (i.e. before PR 2 ships).
- No env changes. Every household ends with exactly one link (today's behavior, unchanged UX).

**Constraints PR 2 MUST carry (from the whole-branch review — easy to lose between PRs):**
- **PR 2 must take the same `SELECT … FOR UPDATE` lock on the household row when CREATING a link.** Min-1 is currently serialized only against concurrent *unlinks*; once link-creation exists, unlink-vs-link races through the same window.
- **PR 2's review brief must name `families.ts`, `aiTools.ts`, and the audit endpoint as in-scope** even though PR 2 won't touch them. PR 2 is what makes multi-link reachable, and PR-1's whole-branch review found isolation leaks living in exactly those files — invisible to per-task review and invisible in PR 2's own diff.

- Spec: `docs/superpowers/specs/2026-07-14-w1-household-family-m2m-design.md` — Steve-approved section-by-section; scope **A+B+C**; locked decisions in §2. **§3.3 amended 2026-07-15** (audit id disclosure — see Work Completed).
- PR-1 plan: `docs/superpowers/plans/2026-07-14-w1-pr1-household-family-m2m-schema-core.md` — 2 Codex council rounds, converged; amended in-branch for the audit response shape.

## Open Blockers / Questions Needing Steve

1. W3a-UI-web spec section 8 UI choices defaulted (per-suggestion admin toggle; elevation notice copy; decline UX). Confirm or change later; no hard blocker.
2. ~~Prod Stripe is in test mode~~ — **DONE 2026-07-02, production billing live** (see Work Completed).
3. ~~**Railway Agent chat secret exposure (found 2026-07-02)**~~ — **RESOLVED 2026-07-06:** all six exposed secrets rotated + verified, old values confirmed dead (see `docs/FamLink_Secrets_Runbook.md` audit log). **Residual pending Steve:** delete the Railway agent conversation itself so the stale paste is gone. Also two optional end-to-end confirmations for the Clerk rotation: a real web login (Vercel new secret key) and a Clerk webhook test event (new signing secret).
4. **Prod auth is on a Clerk TEST instance** (`pk_test_`/`sk_test_`, `moral-marlin-29.clerk.accounts.dev`). Moving to a Clerk production instance requires a real domain.
5. **Guest email delivery blocked on domain:** `RESEND_FROM_DOMAIN=resend.dev` (sandbox — only delivers to the account owner). Needs a real domain verified in Resend; ties into the parked naming decision (same blocker as the Clerk prod instance). SMS delivery is already fully configured.
6. **W3b prod deploy — migration DONE; SMS still pending a verified number.**
   - **(a) Migration `SmsConsent` — DONE, confirmed on prod 2026-07-22** (Railway deploy log: db "railway", no pending migrations). ✅ This is what unblocked PR #12. Reminder for future prod scripts: never migrate/query prod from a bare local shell — root `.env` → local `famlink_dev`; inject the prod `DATABASE_URL` (public proxy `gondola.proxy.rlwy.net:15778`) deliberately.
   - **(b) `API_PUBLIC_URL`** = `https://famlink-api-production.up.railway.app` set in Railway. (Mirroring into Infisical `prod` is **optional** — prod runtime reads Railway, not Infisical; Infisical `prod` is only a mirror/record. 2026-07-25 audit: Infisical `prod` is missing `API_PUBLIC_URL`/`WEB_APP_URL`/`RESEND_FROM_DOMAIN`/`CLERK_PUBLISHABLE_KEY` — harmless, nothing consumes it at runtime.)
   - **(c)–(e) Does NOT gate the merge, only makes SMS function.** Prod's old number `+18556480251` was lost; **replaced by toll-free `+18444235115` ((844) 423-5115) — purchased and `TWILIO_PHONE_NUMBER` set in Railway + Infisical (DONE).** Remaining: confirm the Railway redeploy (from that var change) came up healthy; set the new number's "A message comes in" webhook → `https://famlink-api-production.up.railway.app/api/v1/webhooks/twilio/sms` (HTTP POST); keep Twilio's **default** opt-out ON (**do NOT enable Advanced Opt-Out** — it intercepts STOP before the webhook, so the PENDING→DECLINED sweep never runs); then smoke test STOP → START → Y from a phone with a pending invite. **All US SMS is blocked until toll-free verification is APPROVED (long pole, days–weeks) — see #7.** Also confirm `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN` in prod belong to the same account as the new number.
8. ~~**Rotate the Railway Postgres password**~~ — **DONE 2026-07-24.** Exposed password rotated; prod verified healthy on the new credential (`/health` 200 `db:ok`). **Gotchas hit (record for next rotation):** (a) Railway's Postgres **"Regenerate" rotates the actual role password (`PGPASSWORD`) but does NOT rebuild the static `DATABASE_URL`/`DATABASE_PUBLIC_URL` strings** — they kept the old password → API (referencing `${{Postgres.DATABASE_URL}}`) authenticated with the dead password → **prod 503 `db:error`** until the URL strings were manually updated to the new password. (b) The API's `/health` **is the Railway healthcheck** and it 503s on `db:error`, so a deploy with a bad DB URL **fails the healthcheck and never cuts over** (old container keeps serving 503). (c) We converted the API's `DATABASE_URL` from a literal to a reference (`${{Postgres.DATABASE_URL}}`) during this — good, but the Postgres-side URLs are still **static** (manually spliced), so **they will drift again on the next rotation.** **Hardening DONE 2026-07-25:** recomposed the Postgres `DATABASE_URL`/`DATABASE_PUBLIC_URL` with the password as a `${{PGPASSWORD}}` reference (host/port/user/db left literal — minimal-risk edit since only the password rotates) → **future "Regenerate" rotations now auto-propagate into the URLs, no manual splice.** Verified prod stayed `200`/`db:ok` across the cutover (resolved value identical). Also **deleted the unused `DIRECT_URL`** API var (confirmed prod stayed healthy after — nothing read it). Net: the API's `DATABASE_URL` is a reference to `${{Postgres.DATABASE_URL}}`, and the Postgres URLs reference `${{PGPASSWORD}}`, so the whole chain self-heals on rotation.
7. **Business-identity decisions surfaced by toll-free verification (Steve) — blocks SMS launch, not PR #12.** Toll-free verification needs a **legal entity name**, business website, privacy policy, and a defensible opt-in story; FamLink has none of entity/name/domain settled (same cluster as #4 Clerk-prod + #5 Resend-domain). **Decision:** verify as a **sole proprietor** using Steve's legal name now (toll-free doesn't require an EIN, unlike 10DLC — but the third-party-consent model + a placeholder `vercel.app` site with no privacy policy risks rejection + a weeks-long resubmit), **or** stand up entity + settled name + real domain + privacy policy first and verify once. **Open question for Steve that decides the path: is FamLink launching publicly soon, or still build/test phase?** The SMS use case is **third-party consent** (a member adds another person's phone) — carriers scrutinize this hard; the submission must lean on W3b's built-in compliance (footer, STOP/START/HELP, opt-out suppression). Claude offered to draft the submission.

## Deferred Items (and Why)

- **W3a-UI-web follow-ups (non-blocking, from final review):** (1) **edit-own** task contribution in the foreign viewer (Steve-approved defer; add/delete-own shipped); (2) `ForeignEventDetail` RSVP button can't reflect the viewer's existing RSVP — the foreign DTO doesn't expose self-RSVP (small DTO addition would fix); (3) edit page renders a perpetual loading skeleton if a foreign DTO loads it (unreachable in practice — no link, API rejects; render an "unavailable" state); (4) phone-channel guest delivery lacks a route-level test (per-channel logic is unit-covered); (5) invite suggestion row lost whole-row click-to-toggle (`<label>`→`<div>`; only the checkbox toggles); (6) preview makes 2 sequential DB lookups (could `Promise.all`).
- **W3a-UI-mobile** - after W3a-UI-web (web first). (Contact Identity Foundation is now done, so this sequencing constraint is satisfied.)
- ~~Rest of W3b (passive SMS onboarding)~~ — **DONE 2026-07-14** (PR #11; see Work Completed).
- **W3b backlog (final-review triage, all non-blocking):** (1) extra handler tests — NO-while-suppressed reply suppression, UNSTOP handler-level, STOP-leaves-ACCEPTED-intact, opt-in-only `isPhoneSuppressed=false`; (2) "STOP PLEASE" → UNKNOWN at app layer (exact-word parse; Twilio carrier opt-out is the backstop); (3) dormant `buildEventInviteSmsBody` copy carries `inviterName` — **isolation review is a hard gate before ever reviving `InvitationService.sendEventInvitations`**; (4) cosmetic: TwiML test comment claims a Message-pair count it doesn't assert; garbled fixture comment in `smsInbound.test.ts`.
- **W1 PR-1 backlog (from the whole-branch review, all non-blocking, triaged "defer"):** (1) **ghost residents on family-member removal** — `DELETE /families/:familyId/members/:personId` hard-deletes the `FamilyMember` row but leaves `HouseholdMember`, so an ex-member stays a resident of a household. **Pre-existing; same class as the unlink Ben scenario W1 PR-1 fixed; Steve explicitly declined the scope growth.** Deserves its own ticket. (2) other handlers in `households.ts` still `throw e` from async Express 4 handlers (never reaches `errorHandler` → unhandled rejection / hung request) — pre-existing, but PR-1 slightly widened it (`POST /:id/members` now wraps in `$transaction`, so deadlock/serialization errors are new failure modes hitting that path); worth converting the file wholesale. (3) `householdAdmin` + `actorAdminFamily` are 2 round-trips computing the same thing on 3 paths, making every `if (!actorFamilyGroupId) → 403` guard unreachable; they also still inline `anyLinkedMembership`'s `where` clause (documented). (4) `destroy:true` against a multi-link household silently degrades to a plain unlink + 204 — plan-matching, min-1 preserved, commented; a 409 would be honest. (5) `GET /households/:id` returns 404 before the 403 authz check (cuid ids; file-wide pattern). (6) no index on `HouseholdFamily.linkedByPersonId` / `HouseholdAuditEntry.actorPersonId` (merge-engine `updateMany` table-scans; merge is rare). (7) `FamilyGroup` deletion no longer cascades households — the cascade now lands on `HouseholdFamily`, leaving a household alive with zero links (invisible, min-1 violated at the DB level). **No code path deletes a `FamilyGroup` today**, so theoretical — but it's a lifecycle change the contract migration made silently. (8) web `HouseholdSummary.linkedFamilies?` is optional and not populated by its one current consumer (`GET /:familyId`); PR 2/3 will populate it.
- **W4 (Pro Organizer)** - later in the reframe sequence.
- ~~**Local `.env` consolidation to Infisical**~~ — **DONE + VERIFIED 2026-07-27.** Root `.env` reduced to non-secret local config; `.env.local` blanked (0 keys); secrets now sourced from Infisical `dev` (local) / Railway (prod). Verified full `npm run dev:infisical` brings up **both** apps clean (web 3000, API 3001, `/health` `db:ok`, `.env.local` contributes 0 keys). Surfaced + fixed a port collision: `next dev` honored a global `PORT=3001` → pinned `apps/web` to `next dev -p 3000` (commit `17bc894`) and removed `PORT` from root `.env`. `PORT` was NOT in Infisical (delete 404'd). Model + gotchas documented in `docs/FamLink_Secrets_Runbook.md` → "Local environment architecture". **`NEXT_PUBLIC_*` pruned from Infisical `dev` + VERIFIED 2026-07-27** (injected count 35→27; `dev:infisical` came up clean with no `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY is missing` warning — `apps/web/next.config.mjs` explicitly loads the monorepo-root `.env`, so root `.env` is the authoritative source for the public vars; this also removes the risk of a stale Infisical copy shadowing root `.env`). Remaining optional hygiene: `NODE_ENV`/`WEB_APP_URL` can likewise leave Infisical; placeholder the (likely-revoked, never-committed) Firebase key in `apps/api/.env.test`. **Windows/Turbo gotcha (recurs):** Ctrl+C on a `turbo dev` run orphans the child `node` processes, which keep holding 3000/3001 → next `dev:infisical` fails `EADDRINUSE`; free them with `Get-NetTCPConnection -LocalPort 3000,3001 -State Listen | Select -Expand OwningProcess -Unique | %{ Stop-Process -Id $_ -Force }`.
- **eslint warning cleanup (34)** - non-blocking; do in a sweep later.
- ~~Cloudflare R2 scoped token~~ - **DONE 2026-07-02** (bucket-scoped token live, old revoked; see Work Completed).
- **Billing nav link** - the web app exposes `/billing/plans` and `/settings/billing` but no nav link to them; found during the 2026-07-02 live smoke test. Small UI task.
- **Dev Stripe restricted key re-scope** - the test-mode key's scope (Subscriptions/Customers/Checkout Sessions/Webhooks) is missing Invoices/Customer Portal/Products-read that the API actually uses; re-scope to match `famlink-api-prod-live` when convenient.
- **Secrets manager (Infisical)** - future infra hygiene; do cheap tool-agnostic hardening first (restricted Stripe keys, rotation runbook).
- **Naming** - "KinScape"/"FamScape" taken, "FamLink" contested (Google Family Link); parked pending real USPTO/domain clearance before marketing.

## GitNexus Freshness

Re-analyzed 2026-07-24 on merged `main` (`57901ac`, post-PR-#12 + docs): **2,805 nodes / 4,498 edges / 110 clusters / 116 flows** — fresh, and now the **sole primary index** (single clean `main` slot, `impact`/`detect_changes`/`context` work with no `--branch`). The index now knows W1 PR-1 symbols (`HouseholdFamily`, `householdAccess.ts`, etc.). **Phantom-slot recurrence + fix (worth remembering):** the incremental `node .gitnexus/run.cjs analyze` updated a `main` branch slice but the stale **flat/primary** slot stayed pinned to the long-dead `p3-03-w3a-ui-web` branch (2,504 nodes, 45 behind) — and `clean --branch p3-03-w3a-ui-web` does NOT touch the primary (it only deletes named *branch* slices). The fix that worked: **full `node .gitnexus/run.cjs clean --force`** (wipes the whole `.gitnexus/` dir, runner included) **then re-index** — but since clean removes the runner, re-index with the fallback **`npx --yes gitnexus analyze`**, which rebuilds the current branch as the new primary.

Prior: re-analyzed 2026-07-15 on `main` (`e85e9b4`, the W1 PR-1 checkpoint): **2,782 nodes / 4,419 edges / 104 clusters / 116 flows**. `impact`/`detect_changes` return "not found" for symbols added on an unmerged feature branch — cross-check with `git diff`/grep and say so.

Prior: re-analyzed 2026-07-14 on merged `main` (`de8ed98`, post-PR-#11): **2,735 nodes / 4,290 edges / 118 clusters / 85 flows** — fresh. Note from the W3b build: the index goes stale **inside a worktree** as soon as uncommitted/new files pile up (`detect_changes` produced line-mapping false positives and missed brand-new files all branch long — documented per-task, harmless once known, but don't trust worktree `detect_changes` symbol lists without a `git diff` cross-check).

Prior: re-analyzed 2026-07-06 against `7b59c18` (W3a-UI-mobile complete): 2,679 nodes / 4,277 edges / 103 clusters / 109 flows; the earlier `p3-03-w3a-ui-web` phantom-slot warning no longer appears — `main` is the primary slot again.

Prior note — up-to-date after the 2026-06-29 CIF Plan A production checkpoint. `npx gitnexus analyze` ran after the checkpoint commits: 3,081-3,082 doc-sensitive nodes / 4,255 edges / 98-99 clusters / 89 flows. The one-node variance came from generated adapter metadata text only; execution-flow coverage is unchanged.
